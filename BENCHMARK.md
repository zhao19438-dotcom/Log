# Log 高性能日志系统基准压测评估报告 (Benchmark Report)

本报告对 Log 日志系统在真实物理环境下的性能表现进行标准化评测。评测遵循控制变量法，全面对比【同步直写架构 (`SyncLogger`)】与【双缓冲异步引擎 (`AsyncLogger`)】在高并发日志吞吐、延迟及系统资源消耗方面的表现。

---

## 1. 测试环境 (Test Environment)

为确保压测数据的真实性、严谨性与可复现性，本次基准测试采用的软硬件环境配置如下：

### 1.1 硬件配置
* **CPU 处理器**：AMD Ryzen 7 5800H with Radeon Graphics
  * **架构与核心**：Zen 3 微架构，8 物理核心 / 16 逻辑线程
  * **时钟频率**：基准主频 3.2 GHz，动态加速最高 4.4 GHz
  * **缓存层级**：L1 Cache 512 KB，L2 Cache 4.0 MB，L3 Cache 16.0 MB（统一复合缓存池）
* **系统内存**：16.0 GB DDR4 3200MHz 双通道高速内存
* **持久化存储**：WDC PC SN730 512GB NVMe M.2 SSD（PCIe 3.0 x4，高速顺序连续写入，排除机械寻道瓶颈）

### 1.2 软件配置
* **操作系统**：Microsoft Windows 10 家庭中文版 64 位 (Version 22H2 / OS Build 19045)
* **编译器套件**：MinGW-W64 GCC 13.1.0 (x86_64-posix-seh-rev0)
* **编译构建标准**：ISO C++20 (`-std=c++20`)
* **编译器优化选项**：`-O3 -Wall -g -pthread -I../include`
* **系统电源配置**：高性能插电模式，后台关闭所有无关占载应用

---

## 2. 测试方法 (Test Methodology)

### 2.1 负载模型与测试规格
* **单条消息尺寸**：固定 100 字节的真实业务载荷（Payload），格式化后带时间戳、线程ID、日志等级及模块名。
* **总写入量级**：每项测试固定执行 **1,000,000 条**（一百万条）日志写入，总有效纯日志数据量约 **95.37 MB**。
* **落地策略**：统一持久化至本地磁盘物理文件（`FileSink`，常驻文件流句柄追加写入，消灭频开频关系统调用）。
* **格式控制**：采用精简高性能格式串 `"%m%n"`，最大化体现核心并发引擎的纯吞吐极限。

### 2.2 对比架构设计
1. **同步直写模式 (`SyncLogger`)**：
   * 业务线程直接调用 `submit()` 格式化并加互斥锁写入文件流；
   * 衡量传统直接写盘模式在高并发下的锁竞争与 I/O 阻塞瓶颈。
2. **双缓冲异步引擎 (`AsyncLogger`)**：
   * 业务线程仅以纳秒级 `memcpy` 追加至生产缓冲区（Push Buffer）；
   * 后台消费线程独占读取消费缓冲区（Pop Buffer）批量落盘；
   * 双缓冲通过首地址指针级交换（`std::swap`，耗时约纳秒级）实现生产与消费彻底解耦。

### 2.3 并发梯度与消抖
* **并发线程梯度**：分别在 1 线程、2 线程、3 线程、4 线程下执行全量测试。
* **统计指标**：
  * **总耗时 ($T$)**：所有生产线程并发执行完毕的壁钟时间（Wall-clock Time，单位：秒）。
  * **系统吞吐量 ($\text{QPS}$)**：$\text{QPS} = \frac{1,000,000}{T}$（条/秒）。
  * **数据吞吐带宽 ($\text{Throughput}$)**：$\text{Throughput} = \frac{1,000,000 \times 100}{T \times 1024 \times 1024}$（MB/s）。
  * **单条均摊时延 ($\text{Latency}$)**：$\text{Latency} = \frac{T \times 10^6}{1,000,000}$（微秒/条，$\mu s$）。

---

## 3. 测试结果 (Test Results)

在上述标准环境下，实测 1,000,000 条写入基准压测数据汇总如下：

| 架构模式 | 并发线程数 | 写入总量 | 耗时(秒) | 吞吐量 (QPS) | 数据带宽 (MB/s) | 单条生产时延 ($\mu s$) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **同步直写 (Sync)** | 1 线程 | 1,000,000 | 2.0720s | 482,625 条/秒 | 46.03 MB/s | 2.072 $\mu s$ |
| **同步直写 (Sync)** | 2 线程 | 1,000,000 | 1.8915s | 528,670 条/秒 | 50.42 MB/s | 1.892 $\mu s$ |
| **同步直写 (Sync)** | 3 线程 | 1,000,000 | 2.1710s | 460,623 条/秒 | 43.93 MB/s | 2.171 $\mu s$ |
| **同步直写 (Sync)** | 4 线程 | 1,000,000 | 2.3678s | 422,327 条/秒 | 40.28 MB/s | 2.368 $\mu s$ |
| **双缓冲异步 (Async)** | 1 线程 | 1,000,000 | 1.3209s | **757,072 条/秒** | **72.20 MB/s** | **1.321 $\mu s$** |
| **双缓冲异步 (Async)** | 2 线程 | 1,000,000 | 0.6734s | **1,485,044 条/秒** | **141.62 MB/s** | **0.673 $\mu s$** |
| **双缓冲异步 (Async)** | 3 线程 | 1,000,000 | 0.5795s | **1,725,631 条/秒** | **164.57 MB/s** | **0.579 $\mu s$** |
| **双缓冲异步 (Async)** | 4 线程 | 1,000,000 | 0.4285s | **2,333,659 条/秒** | **222.56 MB/s** | **0.429 $\mu s$** |

---

## 4. 深度架构分析与瓶颈对比

1. **同步写入的“并发逆退”现象**：
   * 从 2 线程增加到 4 线程时，同步直写的 QPS 反而从 52.8 万骤降至 42.2 万（下降约 20%）。
   * **根本原因**：多线程直接争夺同一文件锁。随着竞争线程数增加，绝大部分 CPU 周期浪费在线程休眠与唤醒的上下文切换（Context Switch）中，且每次写磁盘均伴随系统调用开销。
2. **双缓冲异步引擎的“多核加速”优势**：
   * 异步模式下，线程数从 1 增加到 4 时，QPS 从 75.7 万一路飙升至 **233.3 万+ QPS**，数据带宽达 **222.56 MB/s**。
   * **根本原因**：
     - **CPU 空间局部性**：双缓冲区底层基于连续物理内存（`std::vector<char>`），写入过程为连续内存拷贝（`memcpy`），现代 CPU L1/L2 缓存命中率极高；
     - **极短临界区**：仅在缓冲区充满或定时到达时加锁执行指针置换（`std::swap`），锁持有时间低于 10 纳秒；
     - **批量 I/O 下刷**：后台消费线程一次性将上兆字节连续数据批量写入磁盘（Batch Disk I/O），将磁盘带宽利用率发挥到极致。
   * 相比同等 4 线程同步模式，异步引擎的吞吐量提升了 **5.52 倍**，单条日志均摊耗时降低至 **0.429 微秒**！

---

## 5. 复现测试指南 (How to Reproduce)

```bash
# 1. 确保使用支持 C++20 的现代编译器 (推荐 GCC 11+ / 13+)
g++ --version

# 2. 编译压测二进制
make -C bench

# 3. 运行全量自动化压测套件
./bench/bench
```
